+++ BUILTINS
*** Verifies built-in message behavior for "integer as integer" and checks that returned values and side effects match expectations.
!C! 0
!I! 0
>>> 1

class Main : Object {
  run [|
    a := 7 asInteger.
    _ := (a asString) print.
  ]
}
