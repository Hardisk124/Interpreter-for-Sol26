+++ BLOCKS
*** Verifies block semantics for "times repeat", including parameter passing, captured context, and block execution result.
!C! 0
!I! 0
>>> 2

class Main : Object {
  run [|
    _ := 3 timesRepeat: [ :i |
      _ := (i asString) print.
      _ := '\n' print.
    ].
  ]
}
