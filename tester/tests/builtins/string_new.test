+++ BUILTINS
*** Verifies built-in message behavior for "string new" and checks that returned values and side effects match expectations.
!C! 0
!I! 0
>>> 1

class Main : Object {
  run [|
    s := String new.
    _ := ((s length) asString) print.
  ]
}
