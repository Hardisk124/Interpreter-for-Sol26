+++ BUILTINS
*** Verifies built-in message behavior for "string as integer invalid" and checks that returned values and side effects match expectations.
!C! 0
!I! 0
>>> 1

class Main : Object {
  run [|
    n := 'abc' asInteger.
    _ := (n asString) print.
  ]
}
